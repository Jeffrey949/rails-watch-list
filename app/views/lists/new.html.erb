<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%# <%= f.input :photo, as: :file %>
    <%= f.submit %>
  <% end %>
  <p><%= link_to "< Back to all lists", lists_path %></p>
</div>
